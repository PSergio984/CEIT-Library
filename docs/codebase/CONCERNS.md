# Codebase Concerns

## Core Sections (Required)

### 1) Top Risks (Prioritized)

| Severity | Concern | Evidence | Impact | Suggested action |
|----------|---------|----------|--------|------------------|
| High | OpenRouter API key (`LLM_API_KEY`) exposed in an earlier chat session; still live in sidecar `.env` | handoff docs (session record) | Anyone with the key can spend on the account | Rotate the key; it is gitignored and not committed — rotate + update sidecar `.env` only |
| High | `SIDECAR_TOKEN=smoke-test-token` placeholder in both `.env` files | `.env` files (gitignored) | Any local process can call the sidecar; weak shared secret | Set a real token before production (both repos) |
| High | `CEIT ACADEMIC PAPER.xlsx` (real student names, PII) untracked in repo root | `git status` (untracked) | PII leak if swept into a commit (`git add -A`) | Keep untracked; delete or move out of repo |
| Med | `composer audit` advisory `PKSA-365x-2zjk-pt47` ignored in `composer.json` config | `composer.json` `audit.ignore` | Unpatched dependency advisory | Review the advisory; remove ignore when patched |
| Med | LLM cost/rate guards absent (no rate limits, no cost caps) | `.planning/ROADMAP.md` Phase 14 (OPS-02) | Runaway token spend on the OpenRouter path | Phase 14 work: rate limits + cost guards |
| Med | Search freshness depends on scheduler + queue worker running | `routes/console.php`, `app/Jobs/AiIndexRebuildJob.php` | Stale search results if queue not processed | Ensure `schedule:run` + worker in any deploy (Phase 14). Cloud corpus pushes (`ai:push-corpus` hourly :07) need only the scheduler, not the queue |

### 2) Technical Debt

| Debt item | Why it exists | Where | Risk if ignored | Suggested fix |
|-----------|---------------|-------|-----------------|---------------|
| Larastan 3.10 + Laravel 13 relation false positives | Larastan cannot resolve Eloquent relations; 202 errors baselined | `phpstan-baseline.neon` | New relation code silently unchecked (baseline hides it) | Prefer `@property-read` docblocks on new models; regenerate baseline sparingly |
| `phpunit` suite needs frontend build in CI | `@vite` views 500 without `public/build/manifest.json` | `.github/workflows/ci.yml` (npm build step), `resources/views/components/layouts/admin.blade.php` | CI red if manifest missing; local dev can hit it too | Cache `node_modules`; document the requirement (done in `docs/codebase/TESTING.md`) |
| xdebug coverage removed from CI | Ran >20 min on 2-core runners | git history (`ecc63479`, `90f60248`) | No coverage gate | If desired, reintroduce with pcov (3-5x faster) on a dedicated job |
| Stale older docs (`docs/codebase` pre-2026-08-16) claimed PHP 8.2/Laravel 12/PostgreSQL | Generated by an earlier session before upgrades | git history of `docs/codebase/*.md` | Misleading onboarding | Replaced by this set (verified against `composer.json`, CI) |
| `graphify-out/` + `storage/logs/*.log` large files (46 MB browser.log, 33 MB laravel.log) | Debugging output committed/left in tree | scan CODE METRICS | Repo bloat, secrets risk in logs | Prune logs; ensure gitignore covers them |
| Volt installed but no `@volt` usage found | Scaffold default retained | `composer.json` (livewire/volt), scan | Dead dependency | Remove or adopt |
| `REDIS_*`/PostgreSQL env vars unused | Default scaffold | `.env.example` | Confusion | Clean or wire to prod |

### 3) Security Concerns

| Risk | OWASP category (if applicable) | Evidence | Current mitigation | Gap |
|------|--------------------------------|----------|--------------------|-----|
| PII in logs | A09 | `app/Logging/PiiSanitizerProcessor.php` (v1.3) | Global log sanitization processor | Coverage of sanitizer rules not tested [TODO] |
| Weak/placeholder shared secrets | A07 | `.env` files | Token-gated sidecar endpoints | Real token rotation pending |
| Chat data leaving Laravel boundary | A01 | ADR 0006, chat flows | Grounding rules; per-user guard (ADR 0007) | Phase 12/14: role-aware depth + PII-safe copy-level answers; Phase 14 boundary sanitization (OPS-03) |
| Composer advisory | A06 | `composer.json` audit.ignore | Ignored advisory | Re-check patch status |
| CI secrets exposure | A05 | sonar-secrets workflow (passes) | SonarQube secrets scan every push | CodeQL on PRs for all languages (currently on push) |

### 4) Performance and Scaling Concerns

| Concern | Evidence | Current symptom | Scaling risk | Suggested improvement |
|---------|----------|-----------------|-------------|-----------------------|
| Whole-document embeddings + 600-char prompt truncation | sidecar `MAX_DOC_CHARS=600`, `build_context` | Long papers lose detail in prompts | Worse as corpus grows | Document-level chunking (Phase 11 ADR 0013 paper shape; revisit) |
| Sidecar single instance on loopback | sidecar `main.py` (127.0.0.1 bind) | None at current scale | No HA; rebuild blocks rebuilds (global lock) | Phase 14 docker compose deploy |
| SQLite single-file DB | `.env.example`, `phpunit.xml` | Fine at campus scale | Concurrent writes/scale | Evaluate PostgreSQL for prod (env vars already scaffolded) |
| BM25 retrieve-all at corpus scale (`num_results=1_000_000`) | sidecar `search.py` `_bm25_ranks` | Fast at ~100s of docs | Degrades with large corpora | Reintroduce bounded candidate pool with knobs |

### 5) Fragile/High-Churn Areas

| Area | Why fragile | Churn signal | Safe change strategy |
|------|-------------|-------------|----------------------|
| Notification system | Multiple commands/jobs/tests, schema migration history | 3+ superpowers plans 2025-2026 (`notification-preference-casting`, `notification-preferences-schema`, `refactor-notification-system`) | Keep `NotificationService` as the single write path; extend tests per change |
| QR scanner/borrow flow | Security-sensitive (nonce replay, HMAC) + device behavior | `qr-scanner-overhaul`, `atomic-nonce-replay-check` plans | Preserve nonce/HMAC invariants; test on device classes |
| Librarian batch scheduling | Weekday-dependent business rules (Sundays forbidden) | `LibrarianBatchTest` Sunday skip added 2026-08-16 | Freeze time (Carbon) or skip guards in tests |
| AI contract seam (Laravel ↔ sidecar) | Two repos must agree on SSE shape + citation keys | Mirror constants `CITATION_KEYS`/`SSE_CHUNK_KEY` on both sides | Keep mirror constants; extend both test suites together |

### 6) `[ASK USER]` Questions

1. [ASK USER] Production database: SQLite stays, or PostgreSQL (env vars already scaffolded)?
2. [ASK USER] Is a coverage gate wanted again (pcov-based) or is the fast suite the priority?
3. [ASK USER] Should `livewire/volt` be removed (no `@volt` usage found)?
4. [ASK USER] What is the intended prod deployment (Phase 14 docker compose vs current local runs)?

### 7) Evidence

- `.codebase-scan.txt` (CODE METRICS, SECURITY & COMPLIANCE sections)
- `composer.json` (audit.ignore, volt)
- `.env` files (gitignored — SIDECAR_TOKEN placeholder), `.env.example`
- `app/Logging/PiiSanitizerProcessor.php`, sidecar `app/rag.py` (`MAX_DOC_CHARS`)
- `.planning/ROADMAP.md` (Phase 14 OPS-01..04)
