---
phase: 11-academic-papers-agentic-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/CorpusExporter.php
  - tests/Feature/ExportAiCorpusTest.php
autonomous: true
requirements: [SEARCH-05]
must_haves:
  truths:
    - "exported catalog.json paper docs compose text from a SINGLE title occurrence plus authors:/research_adviser:/technical_adviser:/dean:/department:/publication_year:/paper_type:/catalog_code: segments — the Phase 8 title-doubling is gone (D-01, D-02, D-03, ADR 0013)"
    - "metadata block keys are unchanged (catalog_code, department, publication_year int, paper_type, authors array, research_adviser/technical_adviser/dean strings, url /academic-papers/{id}) and the corpus tag stays catalog — the filter + citation contract and ADR 0006 chip rendering are untouched"
    - "schema_version stays 1 — the re-export rides the existing versioned atomic rebuild (ADR 0004 no-contract-change posture)"
  artifacts:
    - path: app/Services/CorpusExporter.php
      provides: "exportCatalog() rich doc text — single title, all-name segments, metadata block verbatim"
      contains: "$segments = [$title.'.'];"
    - path: tests/Feature/ExportAiCorpusTest.php
      provides: "it_exports_catalog_documents_with_locked_schema updated to lock the rich single-title shape"
      contains: "technical_adviser:"
  key_links:
    - from: app/Services/CorpusExporter.php
      to: ceit-ai-sidecar/tests/conftest.py (11-02)
      via: "the same segment template (single title + authors:/research_adviser:/technical_adviser:/dean:/department:/publication_year:/paper_type:/catalog_code:) lands in conftest fixtures in the SAME wave so both sides of the contract agree"
      pattern: "research_adviser: "
    - from: app/Services/CorpusExporter.php
      to: ceit-ai-sidecar/app/search.py (11-02)
      via: "metadata.authors (array) and metadata.research_adviser/technical_adviser (strings) are the fields the new author/adviser filters match against — exporter is the source of truth for those keys"
      pattern: "'authors' => $authors"
---

<objective>
Evolve the catalog corpus document shape (D-01..D-03, ADR 0013): `CorpusExporter::exportCatalog()` composes each paper's `text` from a single title occurrence plus the already-emitted author/adviser/dean/department/paper_type/year/catalog_code segments, with the metadata block and `catalog` corpus tag untouched. Purpose: "topic" becomes free-text search over a rich, paper-specific document, and the author/adviser filter metadata (11-02) stays available — without adding any abstract/summary column (D-01), any taxonomy (D-03), or any contract change (ADR 0004).

Output: `php artisan ai:export-corpus --corpus=catalog` emits rich paper docs; the locked-schema feature test pins the exact shape; the touched Laravel suites stay green.

Commit discipline: one focused commit per task, bundled with its test (Laravel repo only; the sidecar half is 11-02 in the same wave).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-academic-papers-agentic-search/11-CONTEXT.md
@.planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md
@.planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md
@.planning/phases/11-academic-papers-agentic-search/11-VALIDATION.md
@docs/adr/0013-paper-corpus-shape-and-filters.md
@docs/adr/0004-sidecar-chat-endpoint-contract.md
@app/Services/CorpusExporter.php
@tests/Feature/ExportAiCorpusTest.php
@app/Console/Commands/ExportAiCorpus.php
</context>

<threat_model>
ASVS L1. Block on HIGH-severity threats. Trust boundaries relevant to this plan:

| Boundary | From → To | Trust model |
|---|---|---|
| B-1 | Laravel DB → `CorpusExporter` | Trusted: library-owned bibliographic records only (D-01: no abstracts, no user content) |
| B-2 | `CorpusExporter` → `catalog.json` on disk | Trusted: exporter is the only writer; consumed by the sidecar index rebuild |

STRIDE Threat Register:

| ID | STRIDE | Threat | Sev | Disposition | Mitigation in this plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering | Doc-shape drift: composed `text` or metadata keys change shape between the exporter, the locked test, and the sidecar fixtures (11-02 same wave) — search/filter/embedding contract silently breaks | HIGH | MITIGATE | The locked-schema test (`it_exports_catalog_documents_with_locked_schema`) asserts the exact single-title text composition, every metadata key, and the `url`; 11-02's conftest fixtures use the same template (wave-0 coordination); the wave-end eval re-run (11-02 golden set) catches residual drift |
| T-11-02 | Tampering | Corpus tag or `schema_version` changed — forks ADR 0006 chip rendering, availability hydration (`corpus === 'catalog'`), chat corpus validation, similar-books, and the atomic rebuild contract | MED | MITIGATE | Explicit instructions: `'corpus' => 'catalog'` and `schema_version` `1` stay verbatim; the locked-schema test asserts `corpus`/`schema_version`/`metadata` envelope unchanged; the reconcile test (existing `ReconcileAiIndexTest`) must stay green |
| T-11-03 | DoS | Whole-doc embeddings are `doc["text"]` (ingest.py) — a text-shape change shifts every catalog vector and RRF results; unverified quality regressions (e.g., topic queries losing recall) reach production | MED | MITIGATE | Baseline export captured before the change; after the change, re-export + full touched suite; 11-02 adds paper golden cases (author/adviser/topic/year + negatives) and runs `python -m app.eval --corpus catalog` as the wave-end quality gate |

No HIGH-severity threat is left without a mitigation — nothing blocks this plan.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rich paper doc text — single title composition in CorpusExporter::exportCatalog()</name>
  <files>app/Services/CorpusExporter.php</files>
  <read_first>app/Services/CorpusExporter.php (whole exportCatalog() at 12-56 — eager loads at 14-24, the $segments block at 26-34, metadata block at 41-51, sanitize() at 141-154), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 1 — the ONLY change is line 26), .planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (Code Examples 1), docs/adr/0013-paper-corpus-shape-and-filters.md</read_first>
  <action>
  - In `app/Services/CorpusExporter.php`, `exportCatalog()`, change line 26 from `$segments = [$title.'.', $title.'.'];` to `$segments = [$title.'.'];` — the single title occurrence. Leave lines 27-34 verbatim: `authors: ` + `implode('; ', $authors)`, `research_adviser: `, `technical_adviser: `, `dean: `, `department: ` + `sanitize($paper->department, 200)`, `publication_year: ` + `$paper->publication_year`, `paper_type: ` + `sanitize($paper->paper_type, 200)`, `catalog_code: ` + `sanitize($paper->catalog_code, 200)`.
  - Do NOT touch: the eager loads (14-24), the metadata block (41-51 — key names, types, and `url` `/academic-papers/{id}` stay byte-identical), the `'corpus' => 'catalog'` value, `schema_version` (`1`) in `writeEnvelope()` (127-139), or `sanitize()` (141-154). No new imports needed.
  - Behavior contract: the `metadata` block is the filter + citation contract (ADR 0013); only the composed searchable `text` changes.
  </action>
  <behavior>
  GIVEN a seeded AcademicPaper with title T, authors [A1, A2], researchAdviser R, technicalAdviser V, dean D, department Dept, publication_year Y, paper_type P, catalog_code C
  WHEN `(new CorpusExporter)->exportToDisk($path, 'catalog')` runs
  THEN the exported doc's `text` starts with exactly `T. ` (one occurrence, not two) and contains `authors: A1; A2`, `research_adviser: R`, `technical_adviser: V`, `dean: D`, `department: Dept`, `publication_year: Y`, `paper_type: P`, `catalog_code: C`
  AND its `metadata` equals the pre-change shape: catalog_code, department, publication_year (int), paper_type, authors (array), research_adviser, technical_adviser, dean, url = `/academic-papers/{id}`.
  </behavior>
  <verify>php artisan test --filter ExportAiCorpusTest</verify>
  <acceptance_criteria>
  - `app/Services/CorpusExporter.php` line 26 reads exactly `$segments = [$title.'.'];`
  - The file contains the literal `'corpus' => 'catalog'` and `schema_version` unchanged
  - `php artisan test --filter ExportAiCorpusTest` — NOTE: this is a red-green pair. The existing `it_exports_catalog_documents_with_locked_schema` (~line 87) asserts the doubled-title prefix and will be RED after this task's exporter change until Task 2 updates it; both tasks commit together and green only after Task 2 lands.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the rich shape — update it_exports_catalog_documents_with_locked_schema</name>
  <files>tests/Feature/ExportAiCorpusTest.php</files>
  <read_first>tests/Feature/ExportAiCorpusTest.php (seedPaper() at 28-49 — creates all names; readExport() at 51-58; the locked-schema test at 60-91), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 8), .planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (Validation Architecture — D-02 doc shape row)</read_first>
  <action>
  - In `tests/Feature/ExportAiCorpusTest.php`, update `it_exports_catalog_documents_with_locked_schema` (60-91):
    - Replace the title-doubling assertion (`assertStringStartsWith($paper->title.'. '.$paper->title.'. ', ...)`, currently ~line 87) with `assertStringStartsWith($paper->title.'. ', $doc['text'])`.
    - Assert `str_contains($doc['text'], 'authors: '.implode('; ', $seedAuthors))` where `$seedAuthors` is the seeded author-name array — or the literal seeded names; assert `str_contains($doc['text'], 'research_adviser: ...')`, `str_contains($doc['text'], 'technical_adviser: ...')`, `str_contains($doc['text'], 'dean: ...')` with the exact seeded names, plus `'publication_year: '` + year, `'paper_type: '` + type, `'catalog_code: '` + code.
    - Keep the existing metadata-envelope assertions (keys `catalog_code`, `department`, `publication_year` as int, `paper_type`, `authors` as array, `research_adviser`/`technical_adviser`/`dean`, `url` = `/academic-papers/{id}`, `corpus` = `catalog`, `schema_version` = `1`) — assert no metadata key changed and no `abstract`/`summary`/`keywords` key exists.
    - Do NOT change `setUp()` fixture-unlink (20-26), `seedPaper()` (28-49), `readExport()` (51-58), or any other test in the file.
  - Commit discipline: this test update lands in the same commit as Task 1's exporter change (or immediately after, both red-green locked by Task 1's verify).
  </action>
  <behavior>
  GIVEN the updated exporter from Task 1
  WHEN `php artisan test --filter ExportAiCorpusTest` runs
  THEN `it_exports_catalog_documents_with_locked_schema` passes and asserts the single-title text, all eight name/metadata segments, unchanged metadata keys, and the absence of any abstract/summary/keywords key.
  </behavior>
  <verify>php artisan test --filter ExportAiCorpusTest</verify>
  <acceptance_criteria>
  - `tests/Feature/ExportAiCorpusTest.php` no longer contains `$title.'. '.$title` anywhere
  - The test contains `assertStringStartsWith($paper->title.'. ', ...)`, `str_contains` asserts for `'authors: '`, `'research_adviser: '`, `'technical_adviser: '`, `'dean: '`, `'publication_year: '`, `'paper_type: '`, `'catalog_code: '`, and a metadata-envelope assertion including `'corpus' => 'catalog'` and `schema_version`
  - `php artisan test --filter ExportAiCorpusTest` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Re-export the dev corpus and prove the shape on disk + no reconcile drift</name>
  <files>tests/Feature/ReconcileAiIndexTest.php (run only — no code change expected), app/Console/Commands/ExportAiCorpus.php (run only)</files>
  <read_first>.planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (Rebuild ordering pitfall #9; Wave-0 gaps #3/#4), config/services.php (ai_sidecar.corpus_path — default storage_path('app/ai-corpus'))</read_first>
  <action>
  - Run `php artisan ai:export-corpus --corpus=catalog` and verify `storage/app/ai-corpus/catalog.json` exists.
  - Inspect a sample doc (first paper) with a short script: assert `text` begins with exactly one title occurrence and contains all eight segments; assert `metadata.authors` is an array and `metadata.research_adviser`/`technical_adviser`/`dean` are strings; print the doc `id` + first 120 chars of `text` as the evidence line.
  - Run `php artisan test --filter ReconcileAiIndexTest` — the existing nightly-reconcile test must stay green with the new doc shape (it asserts counts/coverage, not text).
  - Run the two touched suites together: `php artisan test --filter 'ExportAiCorpusTest|ReconcileAiIndexTest'` — exits 0.
  - Do NOT trigger a sidecar rebuild or touch any file in the sidecar repo (C:\Users\admin\Herd\ceit-ai-sidecar is a SEPARATE git repo — 11-02 owns it; the eval quality gate runs there at wave end).
  </action>
  <verify>php artisan test --filter 'ExportAiCorpusTest|ReconcileAiIndexTest'</verify>
  <acceptance_criteria>
  - `storage/app/ai-corpus/catalog.json` exists and its first doc's `text` starts with exactly one title occurrence followed by `authors:` (evidence line printed in the task output)
  - `php artisan test --filter 'ExportAiCorpusTest|ReconcileAiIndexTest'` exits 0 with no failures
  - No sidecar-repo files were modified by this plan
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `php artisan test --filter 'ExportAiCorpusTest|ReconcileAiIndexTest'` — green
- [ ] `php artisan test` — full Laravel suite green (585 passed / 3 skipped baseline, no new failures)
- [ ] `storage/app/ai-corpus/catalog.json` re-exported; sampled doc shows single-title rich text with all 8 segments and unchanged metadata keys
- [ ] No `abstract`/`summary`/`keywords` key, no `papers` corpus tag, `schema_version` = 1 (ADR 0013 contract intact)
</verification>

<success_criteria>
- Task 1-2 complete: exporter emits rich paper docs and the locked-schema test pins the shape (SEARCH-05 topic-search foundation, D-01..D-03)
- Task 3 complete: dev corpus re-exported, reconcile suite green — the wave-0 contract with 11-02's conftest fixtures is in place
- Catalog corpus tag and metadata contract untouched — ADR 0006 chips, availability hydration, and similar-books keep working
</success_criteria>

<output>
After completion, create `.planning/phases/11-academic-papers-agentic-search/11-01-SUMMARY.md`
</output>
