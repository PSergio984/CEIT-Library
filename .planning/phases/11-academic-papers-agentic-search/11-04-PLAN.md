---
phase: 11-academic-papers-agentic-search
plan: 04
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - app/Livewire/Pages/Student/AcademicPaperIndex.php
  - resources/views/livewire/pages/student/academic-paper-index.blade.php
  - resources/views/components/academic-paper-filters.blade.php
  - tests/Feature/AcademicPaperIndexHybridTest.php
autonomous: true
requirements: [SEARCH-05]
must_haves:
  truths:
    - "Paper search is a tab/mode on the existing student AcademicPaperIndex page — Browse / Paper Search tab strip, no route/URL change; tab switches are pure prop sets that NEVER reset search/filters/results (D-04, UI-SPEC §Paper Search Tab)"
    - "author/adviser are included in the sidecar payload ONLY in paper-tab mode; browse-mode payloads carry exactly the Phase 9/10 five filter keys — existing payload tests stay green (UI-SPEC composition matrix, ADR 0013)"
    - "Status filter force-exit (updatedStatusFilter → exitHybridMode) honored inside the tab; author/adviser refine-only (no force-exit); results stay in sidecar RRF order with no sort control, 'Results ranked by relevance' caption in tab mode (D-05, D-06)"
  artifacts:
    - path: app/Livewire/Pages/Student/AcademicPaperIndex.php
      provides: "authorFilter/adviserFilter/paperTabActive props, availableAuthors/availableAdvisers #[Computed]s, updated handlers, conditional filter passthrough, clearFilters + recommendationsSnapshot extended"
      contains: "paperTabActive"
    - path: resources/views/livewire/pages/student/academic-paper-index.blade.php
      provides: "Browse/Paper Search tab strip, paper-tab idle empty state, relevance caption, wire:target additions"
      contains: "Paper Search"
    - path: resources/views/components/academic-paper-filters.blade.php
      provides: "author/adviser selects (gated on $wire->paperTabActive), Alpine data wiring, active-filter badges + clear"
      contains: "authorFilter"
  key_links:
    - from: app/Livewire/Pages/Student/AcademicPaperIndex.php
      to: ceit-ai-sidecar/app/search.py (11-02)
      via: "the payload filter keys author/adviser are exactly the keys the sidecar passes() clauses read — key drift would silently return unfiltered results (permissive dict)"
      pattern: "'adviser' =>"
    - from: resources/views/components/academic-paper-filters.blade.php
      to: app/Livewire/Pages/Student/AcademicPaperIndex.php
      via: "select options come from availableAuthors/availableAdvisers computeds (Author + ResearchAdviser/TechnicalAdviser name tables) — exact names the substring filter matches"
      pattern: "availableAdvisers"
    - from: app/Livewire/Pages/Student/AcademicPaperIndex.php
      to: app/Services/AiService.php
      via: "AiService::search($query, $filters, 'catalog', 10) forwards filters verbatim — zero service changes; capture tests prove the payload"
      pattern: "AiService::search"
---

<objective>
Deliver the SEARCH-05 search surface (D-04..D-06, UI-SPEC): a Browse / Paper Search tab strip on the existing student `AcademicPaperIndex` page, explicit author + adviser selects (plus the existing free-text topic box and year-range controls), all routed through the existing hybrid-search path (`AiService::search(..., 'catalog', 10)` — zero service changes), with the sidecar RRF rank order preserved and no sort control.

Purpose: users can search papers by topic, author, year, or adviser and open a paper's catalog page from the result (ROADMAP success criterion 1) — reusing the existing card grid, detail modal, availability hydration, and hybrid-search machinery.

Output: paper tab live on the page; sidecar payload capture tests green; browse-mode behavior byte-identical to today.

Commit discipline: one focused commit per task, tests bundled with their code (Laravel repo).
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-academic-papers-agentic-search/11-CONTEXT.md
@.planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md
@.planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md
@.planning/phases/11-academic-papers-agentic-search/11-UI-SPEC.md
@.planning/phases/11-academic-papers-agentic-search/11-VALIDATION.md
@docs/adr/0013-paper-corpus-shape-and-filters.md
@docs/adr/0012-similar-button-ux.md
@app/Livewire/Pages/Student/AcademicPaperIndex.php
@resources/views/livewire/pages/student/academic-paper-index.blade.php
@resources/views/components/academic-paper-filters.blade.php
@tests/Feature/AcademicPaperIndexHybridTest.php
@tests/Feature/AcademicPaperIndexSimilarTest.php
</context>

<threat_model>
ASVS L1. Block on HIGH-severity threats. Trust boundaries relevant to this plan:

| Boundary | From → To | Trust model |
|---|---|---|
| B-1 | Browser (student) → AcademicPaperIndex page | Authenticated student session; Livewire validation rules on every filter prop |
| B-2 | AcademicPaperIndex → sidecar /search | Token-authenticated; closed top-level schema; filters dict permissive by design (ADR 0004) |

STRIDE Threat Register:

| ID | STRIDE | Threat | Sev | Disposition | Mitigation in this plan |
|---|---|---|---|---|---|
| T-11-14 | Spoofing | Filter-key drift: the page posts filter keys the sidecar does not accept → permissive dict silently ignores them → users get unfiltered/wrong results with no error | MED | MITIGATE | Exact key strings `author`/`adviser` locked in the capture test (`it_searches_with_author_and_adviser_filters_in_tab_mode` asserts `$request['filters']` contains exactly those keys); sidecar keys come from the same ADR 0013 table 11-02 implemented |
| T-11-15 | Tampering | Stale author/adviser values leak into browse-mode payloads, changing Phase 9/10 result behavior (browse-mode filter keys must stay byte-identical) | MED | MITIGATE | author/adviser appended to the filters array ONLY when `paperTabActive` — browse mode posts exactly the five existing keys (back-compat test `it_does_not_send_author_adviser_keys_outside_paper_tab`); clearFilters() resets the two new props |
| T-11-16 | Spoofing | Oversized/invalid filter values (Livewire props are user-controlled strings) abuse the sidecar or pollute results | MED | MITIGATE | `#[Validate('string|max:100|nullable')]` on both new props (copy of the existing validated-filter block); values forwarded verbatim as strings, matching the sidecar str()-guarded clauses |
| T-11-17 | Tampering | Mode-composition regression: status-filter force-exit bypassed in tab mode, or tab switch clobbers recommendations/browse state | MED | MITIGATE | `updatedStatusFilter()` → `exitHybridMode()` untouched; tab clicks are pure `$set('paperTabActive', ...)` with no reset call; recommendationsSnapshot extended with the new props; tests assert state preservation (Http::recorded() unchanged across tab switches) |

No HIGH-severity threat is left without a mitigation — nothing blocks this plan.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AcademicPaperIndex — props, computeds, handlers, conditional filter passthrough, snapshot extension</name>
  <files>app/Livewire/Pages/Student/AcademicPaperIndex.php</files>
  <read_first>app/Livewire/Pages/Student/AcademicPaperIndex.php (validated-filter props block 41-57, computeds 188-224 — copy availableDepartments() 204-213 shape, updatedStatusFilter 239-247, updatedYearFilter-style handlers 249-254, clearFilters 285-297, runHybridSearch 303-356 with the filters array at 311-317, recommendationsSnapshot 65-73 + showSimilar/backToResults 376-434), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 3), .planning/phases/11-academic-papers-agentic-search/11-UI-SPEC.md (§Paper Search Tab, §Filter Controls), docs/adr/0013-paper-corpus-shape-and-filters.md</read_first>
  <action>
  - Add public props next to the existing validated-filter block (41-57):
    - `#[Validate('string|max:100|nullable')] public string $authorFilter = '';`
    - `#[Validate('string|max:100|nullable')] public string $adviserFilter = '';`
    - `public bool $paperTabActive = false;`
  - Add `#[Computed(persist: true, cache: true)]` computeds copying `availableDepartments()` (204-213):
    - `availableAuthors()`: `Author::distinct()->orderBy('name')->pluck('name')->filter()->values()` (import `App\Models\Author`).
    - `availableAdvisers()`: `collect(ResearchAdviser::orderBy('name')->pluck('name'))->concat(TechnicalAdviser::orderBy('name')->pluck('name'))->unique()->sort()->values()` (imports `App\Models\ResearchAdviser`, `App\Models\TechnicalAdviser` — verify the exact model class names against `app/Models/` before importing).
  - `runHybridSearch()` filters array (311-317): keep the five existing keys verbatim; AFTER them append `'author' => $this->authorFilter ?: null` and `'adviser' => $this->adviserFilter ?: null` wrapped in `if ($this->paperTabActive) { ... }` — browse-mode payloads stay byte-identical to today. Do NOT change the `AiService::search($this->search, $filters, 'catalog', 10)` call or the >=3-char gate (305-309).
  - Add handlers copying the updatedYearFilter-style shape (249-254): `updatedAuthorFilter()` and `updatedAdviserFilter()` → `$this->exitRecommendationsMode(); $this->resetPage(); $this->runHybridSearch();` — they MUST NOT force-exit hybrid mode (refine-only, UI-SPEC §Paper Search Tab).
  - `clearFilters()` (285-297): add `'authorFilter', 'adviserFilter'` to the `reset([...])` list.
  - `recommendationsSnapshot` (65-73, set in `showSimilar()` 376-434): add `'authorFilter' => $this->authorFilter, 'adviserFilter' => $this->adviserFilter, 'paperTabActive' => $this->paperTabActive` to the snapshot array; restore them in the `backToResults()` restore block. Do NOT add an enter/exit hook on `updatedPaperTabActive` — tab switches are pure prop sets (no reset, no re-query).
  </action>
  <behavior>
  GIVEN `paperTabActive = true`, `search = "solar"`, `authorFilter = "Juan Dela Cruz"`, `adviserFilter = "Engr. Jose Santos"`
  WHEN `runHybridSearch()` executes
  THEN the sidecar payload's `filters` contains `author` and `adviser` keys with those values (plus the five existing keys)
  GIVEN `paperTabActive = false` with the same filter props still set
  WHEN `runHybridSearch()` executes
  THEN the payload's `filters` contains EXACTLY the five Phase 9/10 keys — no author/adviser keys
  GIVEN the user sets `statusFilter` while `paperTabActive = true`
  THEN hybrid mode exits (hybridResults null, SQL path renders) — the force-exit is honored inside the tab
  GIVEN the user toggles the tab
  THEN no reset, no HTTP call, no state loss (pure prop set).
  </behavior>
  <verify>php artisan test --filter AcademicPaperIndexHybridTest</verify>
  <acceptance_criteria>
  - `app/Livewire/Pages/Student/AcademicPaperIndex.php` contains `public string $authorFilter = '';`, `public string $adviserFilter = '';`, `public bool $paperTabActive = false;`, `availableAuthors()`, `availableAdvisers()`, `updatedAuthorFilter()`, `updatedAdviserFilter()`
  - The filters array in `runHybridSearch()` appends author/adviser inside a `paperTabActive` conditional
  - `clearFilters()` reset list contains `authorFilter` and `adviserFilter`; `recommendationsSnapshot` contains the three new keys
  - `php artisan test --filter AcademicPaperIndexHybridTest` passes (existing browse-mode payload tests unchanged and green)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Blade — tab strip, idle empty state, relevance caption, wire:targets + author/adviser selects in the shared filters component</name>
  <files>resources/views/livewire/pages/student/academic-paper-index.blade.php, resources/views/components/academic-paper-filters.blade.php</files>
  <read_first>resources/views/livewire/pages/student/academic-paper-index.blade.php (header 25-28, filters component call 31-35 with the availableYears/Departments/PaperTypes wiring, hybrid result areas 237-426 mobile / 439-522 desktop, empty states ~320-326, wire:target lists at ~240 and ~432), resources/views/components/academic-paper-filters.blade.php (Alpine props 9-12, init 14-19, grid 91, department select 108-113, hasActiveFilters 21-23, badges 133-155), resources/views/livewire/pages/admin/admin-advisers-deans.blade.php (tab strip pattern 32-53), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (sections 4-5), .planning/phases/11-academic-papers-agentic-search/11-UI-SPEC.md (verbatim markup for the tab strip, selects, empty state, and the copy table)</read_first>
  <action>
  - `academic-paper-index.blade.php`:
    - Insert the tab strip between the header block and `<x-academic-paper-filters>` (UI-SPEC §Paper Search Tab verbatim): `<div role="tablist" class="tabs tabs-boxed mb-6 bg-base-200 w-max">` with two `<a role="tab">` items — `Browse` (`wire:click="$set('paperTabActive', false)"`) and `Paper Search` (`wire:click="$set('paperTabActive', true)"`), `tab-active` toggled on `! $paperTabActive` / `$paperTabActive`.
    - Extend the filters component call (31-35): pass `:availableAuthors="$this->availableAuthors"` and `:availableAdvisers="$this->availableAdvisers"`.
    - Append `paperTabActive, authorFilter, adviserFilter` to BOTH `wire:target` overlay lists (~line 240 and ~line 432).
    - Paper-tab idle empty state: in the mobile hybrid `@empty` and desktop equivalents, when `$paperTabActive && ! $search && ! $authorFilter && ! $adviserFilter` render `<x-empty-state icon="o-magnifying-glass" title="Search the paper collection" message="Type a topic, or choose an author or adviser to find papers." :show-action="false" size="sm" />` (mobile) / `size="default"` (desktop) instead of the "No Academic Papers Found" block; when topic or filters ARE set, the existing "No Academic Papers Found" copy stays.
    - Relevance caption: when `$paperTabActive && $hybridResults` render the muted caption `Results ranked by relevance` (`text-xs text-base-content/60`) above the hybrid results — no sort control anywhere (D-06).
  - `academic-paper-filters.blade.php` (shared with admin — gate on `$wire->paperTabActive`):
    - Alpine props (9-12): add `availableAuthors: null, availableAdvisers: null`; `init()` (14-19): assign `this.availableAuthors` / `this.availableAdvisers` from the passed props (copy the existing three assignments).
    - After the Department select inside the grid (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3`), add two selects wrapped in `@if ($wire->paperTabActive)` (UI-SPEC verbatim): `wire:model.live="authorFilter"` with `<option value="">All Authors</option>` + `<template x-for="author in availableAuthors" :key="author">` options; identical for `adviserFilter` / `All Advisers` / `availableAdvisers`. Classes `select select-bordered select-sm sm:select-md w-full`.
    - `hasActiveFilters` getter (21-23): add `|| $wire->authorFilter || $wire->adviserFilter`.
    - Badge row (133-155): add `Author: {name}` / `Adviser: {name}` `badge badge-sm` spans bound to the two props.
    - Do NOT change the status/type/department/year selects or the year-from/to Alpine `validYearsFrom`/`validYearsTo` behavior.
  </action>
  <verify>php artisan test --filter AcademicPaperIndexHybridTest; php artisan view:cache</verify>
  <acceptance_criteria>
  - `academic-paper-index.blade.php` contains the tab strip with `Browse` and `Paper Search` literals, `wire:click="$set('paperTabActive', ...)"` on both, the `Search the paper collection` idle empty state, the `Results ranked by relevance` caption, `:availableAuthors` / `:availableAdvisers` on the filters component, and `paperTabActive, authorFilter, adviserFilter` in both `wire:target` lists
  - `academic-paper-filters.blade.php` contains `All Authors` and `All Advisers` options, `wire:model.live="authorFilter"` / `wire:model.live="adviserFilter"`, the `@if ($wire->paperTabActive)` gate, and the two new badge cases
  - `php artisan view:cache` compiles without Blade errors; `php artisan test --filter AcademicPaperIndexHybridTest` still green
  </acceptance_criteria>
</task>

  <task type="auto" tdd="true">
  <name>Task 3: Feature tests — tab-mode filter forwarding, browse back-compat, force-exit, state preservation</name>
  <files>tests/Feature/AcademicPaperIndexHybridTest.php, tests/Feature/AcademicPaperIndexSimilarTest.php (pattern only), tests/Feature/AiServiceTest.php</files>
  <read_first>tests/Feature/AcademicPaperIndexHybridTest.php (the whole file — seed/act-arrange setup, it_forwards_filter_values_to_the_sidecar 121-140 with Http::fake capture, force-exit case 182-210), tests/Feature/AcademicPaperIndexSimilarTest.php (snapshot/restore + Http::recorded() patterns 122-153), tests/Feature/AiServiceTest.php (Http::fake capture pattern 21-46 — asserts top-level payload keys exactly query/filters/corpus/limit/k), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 12), .planning/phases/11-academic-papers-agentic-search/11-VALIDATION.md (D-04 rows)</read_first>
  <action>
  - In `tests/Feature/AcademicPaperIndexHybridTest.php` add, copying the existing capture-test shape (121-140 — `Http::preventStrayRequests()`, fake `search.json`, `Http::assertSent` closure over `$request->data()`):
    - `it_searches_with_author_and_adviser_filters_in_tab_mode`: `Livewire::test(AcademicPaperIndex::class)->set('paperTabActive', true)->set('search', 'solar')->set('authorFilter', 'Juan Dela Cruz')->set('adviserFilter', 'Engr. Jose Santos')` → assert the captured `$request['filters']` contains exactly the five existing keys PLUS `'author' => 'Juan Dela Cruz'` and `'adviser' => 'Engr. Jose Santos'`; results render in sidecar rank order; the `availability()` computed is present on the returned models (shared card path).
    - `it_does_not_send_author_adviser_keys_outside_paper_tab`: same setup WITHOUT `paperTabActive` → captured `$request['filters']` equals exactly the five Phase 9/10 keys (assert array keys === `['paper_type','department','publication_year','year_from','year_to']`), even with author/adviser props set.
    - `it_exits_paper_tab_hybrid_on_status_filter`: `paperTabActive` true + search set → `set('statusFilter', 'Available')` → `hybridResults` null and the SQL path renders (force-exit honored in tab mode).
    - `it_preserves_state_across_tab_switches`: set search + author/adviser + results, record `Http::recorded()` count, then `set('paperTabActive', false)` then back to `true` → assert recorded count unchanged (no re-query) and props/results intact.
    - `it_restores_author_adviser_after_recommendations`: extend the similar-mode round-trip: set tab state + filters → `showSimilar($paperId)` → `backToResults()` → authorFilter/adviserFilter/paperTabActive restored to pre-similar values, `Http::recorded()` unchanged during restore.
  - In `tests/Feature/AiServiceTest.php` add (service-level payload-key assertion — closes the PATTERNS.md ownership gap, complementing the page-level capture above):
    - `it_passes_author_and_adviser_filter_keys_to_the_sidecar`: `(new AiService)->search('solar', ['author' => 'Juan Dela Cruz', 'adviser' => 'Engr. Jose Santos'])` with `Http::fake` → `Http::assertSent` closure asserts the captured `$request->data()` top-level keys are EXACTLY `['query', 'filters', 'corpus', 'limit', 'k']` and `$request['filters']` carries the two new keys (copying the AiServiceTest.php 21-46 capture pattern).
  </action>
  <behavior>
  GIVEN the paper tab active with topic + author + adviser set
  WHEN the hybrid search fires
  THEN the captured sidecar payload carries author + adviser keys inside filters and results render in rank order with availability
  GIVEN the same filters but tab inactive
  THEN the captured payload carries exactly the five legacy keys
  GIVEN tab switches / similar-mode round-trips
  THEN zero additional HTTP calls and full state restoration.
  </behavior>
  <verify>php artisan test --filter AcademicPaperIndexHybridTest; php artisan test --filter AcademicPaperIndexSimilarTest</verify>
  <acceptance_criteria>
  - `tests/Feature/AcademicPaperIndexHybridTest.php` contains the five new tests named above
  - `php artisan test --filter AcademicPaperIndexHybridTest` exits 0; `php artisan test --filter AcademicPaperIndexSimilarTest` exits 0 (snapshot changes did not break the similar-mode suite)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `php artisan test --filter 'AcademicPaperIndexHybridTest|AcademicPaperIndexSimilarTest|AiServiceTest'` — green
- [ ] `php artisan test` — full Laravel suite green (585 passed / 3 skipped baseline, no new failures)
- [ ] `php artisan view:cache` — Blade compiles
- [ ] Manual smoke (browser, optional but recommended): paper tab shows idle prompt, selecting Author/Adviser re-runs hybrid with overlay, Browse tab returns identical pre-tab results, status filter drops to SQL list inside the tab
</verification>

<success_criteria>
- Paper tab live: topic box + author + adviser + year-range filters on the student page; results in sidecar RRF order with the relevance caption; no sort control (D-04..D-06)
- Browse mode and admin page byte-identical to before (author/adviser gated to the tab; payload capture tests green)
- Users can open a paper's catalog page (detail modal) from paper-tab results — ROADMAP success criterion 1, SEARCH-05
</success_criteria>

<output>
After completion, create `.planning/phases/11-academic-papers-agentic-search/11-04-SUMMARY.md`
</output>
