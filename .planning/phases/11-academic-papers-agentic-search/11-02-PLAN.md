---
phase: 11-academic-papers-agentic-search
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ceit-ai-sidecar/app/search.py
  - ceit-ai-sidecar/tests/conftest.py
  - ceit-ai-sidecar/tests/test_filters.py
  - ceit-ai-sidecar/tests/test_api.py
  - ceit-ai-sidecar/data/golden_dataset.json
autonomous: true
requirements: [SEARCH-05]
must_haves:
  truths:
    - "rrf_search passes() accepts filters author (case-insensitive substring over metadata.authors array) and adviser (case-insensitive substring over metadata.research_adviser OR metadata.technical_adviser) — additive clauses, pre-fusion application, str()-guarded values (D-05, ADR 0013)"
    - "POST /search with author/adviser inside filters returns 200 + filtered results; unknown TOP-LEVEL fields still 422 invalid_request — SEARCH_ALLOWED_KEYS untouched, closed-schema posture unchanged (A-5, ADR 0004)"
    - "sidecar conftest corpus docs use the SAME rich single-title text template and carry research_adviser/technical_adviser/dean metadata keys — wave-0 agreement with 11-01's exporter shape"
  artifacts:
    - path: ceit-ai-sidecar/app/search.py
      provides: "AUTHOR_KEYS tuple + author/adviser clauses inside passes()"
      contains: "AUTHOR_KEYS"
    - path: ceit-ai-sidecar/tests/test_filters.py
      provides: "test_filter_author_any_of_multiple + test_filter_adviser_research_or_technical"
      contains: "test_filter_author_any_of_multiple"
    - path: ceit-ai-sidecar/tests/test_api.py
      provides: "test_search_endpoint_accepts_author_adviser_filters + unchanged top-level 422 posture test"
      contains: "test_search_endpoint_accepts_author_adviser_filters"
    - path: ceit-ai-sidecar/data/golden_dataset.json
      provides: "papers cases (author/adviser/year/topic + 2 negatives) and a refreshed catalog_snapshot"
      contains: "author"
  key_links:
    - from: ceit-ai-sidecar/tests/conftest.py
      to: ceit-ai-sidecar/app/search.py
      via: "filter tests exercise real metadata fields — conftest make_corpus docs must carry metadata.authors + research_adviser/technical_adviser before passes() tests assert them"
      pattern: "research_adviser"
    - from: ceit-ai-sidecar/app/search.py
      to: app/Services/CorpusExporter.php (11-01)
      via: "passes() reads exactly the metadata keys the exporter writes (authors array, research_adviser/technical_adviser strings) — the exporter is the source of truth for filterable fields"
      pattern: "meta.get(\"authors\")"
    - from: ceit-ai-sidecar/app/search.py
      to: app/Livewire/Pages/Student/AcademicPaperIndex.php (11-04)
      via: "the filter key names author/adviser are the contract the paper tab posts — both sides must use exactly these strings"
      pattern: "'author'"
---

<objective>
Deliver the SEARCH-05 filter half in the sidecar (D-05, ADR 0013): add `author` (matches any element of `metadata.authors`, case-insensitive substring) and `adviser` (matches `metadata.research_adviser` OR `metadata.technical_adviser`, case-insensitive substring) to `rrf_search`'s post-retrieval `passes()`, prove the endpoint accepts them inside the permissive `filters` dict while the top-level 422 posture stays, update conftest corpus fixtures to the rich single-title doc shape (wave-0 agreement with 11-01's exporter), and land the papers quality-gate golden cases with a refreshed snapshot.

Purpose: topic/author/year/adviser search works end to end once 11-04's paper tab posts these keys; the golden cases make the wave-end `python -m app.eval --corpus catalog` run a real quality gate (RESEARCH Validation Architecture).

Output: sidecar suite green (49 passed / 1 skipped baseline + new tests), ruff clean, golden set extended.

Commit discipline: sidecar is a SEPARATE git repo at C:\Users\admin\Herd\ceit-ai-sidecar — commit every task with `git -C C:\Users\admin\Herd\ceit-ai-sidecar add ...` + `git -C C:\Users\admin\Herd\ceit-ai-sidecar commit`; one focused commit per task, tests bundled with their code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-academic-papers-agentic-search/11-CONTEXT.md
@.planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md
@.planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md
@.planning/phases/11-academic-papers-agentic-search/11-VALIDATION.md
@docs/adr/0013-paper-corpus-shape-and-filters.md
@docs/adr/0004-sidecar-chat-endpoint-contract.md
@ceit-ai-sidecar/app/search.py
@ceit-ai-sidecar/tests/conftest.py
@ceit-ai-sidecar/tests/test_filters.py
@ceit-ai-sidecar/tests/test_api.py
@ceit-ai-sidecar/app/eval.py
@ceit-ai-sidecar/data/golden_dataset.json
</context>

<threat_model>
ASVS L1. Block on HIGH-severity threats. Trust boundaries relevant to this plan:

| Boundary | From → To | Trust model |
|---|---|---|
| B-1 | Laravel `AiService::search()` → sidecar `/search` | Token-authenticated (X-Sidecar-Token), closed top-level schema — `filters` dict intentionally permissive (ADR 0004) |
| B-2 | Agentic loop tool args (11-03) → `rrf_search()` filters | UNTRUSTED model output becomes search input — every value must be type-guarded before use |
| B-3 | Exported `catalog.json` → index rebuild → `passes()` metadata | Trusted: exporter-written metadata keys are the contract filters read |

STRIDE Threat Register:

| ID | STRIDE | Threat | Sev | Disposition | Mitigation in this plan |
|---|---|---|---|---|---|
| T-11-04 | Tampering | Junk agent tool args (e.g. `author` as list/dict) reach `passes()` and raise TypeError → 500 on /search | MED | MITIGATE | Every new filter value wrapped in `str(...)` before `.lower()`/containment (search.py pattern already used for year filters); unit tests pass non-str junk values and assert clean False, never an exception (pitfall #5) |
| T-11-05 | Tampering | Top-level closed-schema posture regresses: a stray top-level key stops 422ing, or `SEARCH_ALLOWED_KEYS` gains a key | MED | MITIGATE | Explicit instruction: `SEARCH_ALLOWED_KEYS` (main.py:59) untouched — new keys ride inside the permissive `filters` dict only; `test_search_endpoint_accepts_author_adviser_filters` re-asserts the unknown-top-level-field 422 in the same commit |
| T-11-06 | DoS | Golden-set quality gate degrades silently (snapshot drift, cases with wrong expected ids) so wave-end eval gives a false pass/fail | LOW | MITIGATE | `catalog_snapshot` refreshed in the same commit as the cases; expected ids derived from the ACTUAL exported corpus (`storage/app/ai-corpus/catalog.json` ids); eval run is part of task verification, not just wave-end |
| T-11-07 | Info disclosure | Filter error paths leak internal state (doc shapes, index internals) in 422/500 envelopes | LOW | MITIGATE | No new error paths: invalid filter values return filtered-empty results (fail-closed False), never a diagnostic message; 422 envelope stays the existing `_invalid()` shape |

No HIGH-severity threat is left without a mitigation — nothing blocks this plan.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rich-text conftest corpus fixtures (wave-0 coordination with 11-01)</name>
  <files>ceit-ai-sidecar/tests/conftest.py</files>
  <read_first>ceit-ai-sidecar/tests/conftest.py (make_corpus at 43-132, doc text shapes at 58/71/84/97, DeterministicEmbedder 22-40, build_test_index 165-172), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 16), app/Services/CorpusExporter.php (the exact segment template 11-01 keeps), .planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (Code Examples 1 + Wave-0 gaps #1)</read_first>
  <action>
  - In `C:\Users\admin\Herd\ceit-ai-sidecar\tests\conftest.py`, `make_corpus()`, rewrite each paper doc's `text` to the rich single-title template — drop the doubled title, keep a single title occurrence followed by `authors: ` (semicolon-joined), `research_adviser: `, `technical_adviser: `, `dean: `, `department: `, `publication_year: `, `paper_type: `, `catalog_code: ` segments — exactly mirroring CorpusExporter.php lines 26-34.
  - Add to each doc's `metadata`: `"research_adviser"` (str), `"technical_adviser"` (str), `"dean"` (str) keys with distinct fixture names per doc (e.g. doc 1 research_adviser "Engr. Jose Santos", another doc technical_adviser "Engr. Maria Reyes") so adviser filter tests discriminate research-vs-technical matches.
  - Keep `metadata.authors` as a list; keep `corpus` = `catalog`, `id` = `paper-N`, `title`, `url` as today.
  - Do NOT touch DeterministicEmbedder (22-40), `embed_from` (150-162), `build_test_index` (165-172), or any fixture not listed above.
  - This must land in the same execution wave as 11-01's exporter change (wave-0 requirement) — coordinate via the shared template, not by file coupling.
  </action>
  <behavior>
  GIVEN the updated conftest corpus
  WHEN any existing filter test (test_filters.py) runs
  THEN no test fails due to text/metadata shape changes (existing tests assert metadata keys, not the doubled-title text), AND `metadata.research_adviser`/`technical_adviser`/`dean` are present as strings.
  </behavior>
  <verify>cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run pytest tests/test_filters.py tests/test_rrf.py -x</verify>
  <acceptance_criteria>
  - `ceit-ai-sidecar/tests/conftest.py` contains NO doubled-title text (`title. title` pattern gone from make_corpus)
  - Every make_corpus doc contains `"research_adviser"`, `"technical_adviser"`, `"dean"` metadata keys; at least one doc differs between research_adviser and technical_adviser values
  - `uv run pytest tests/test_filters.py tests/test_rrf.py -x` exits 0 (existing tests still green with the rich fixtures)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: author/adviser filter clauses in passes() + engine/API tests</name>
  <files>ceit-ai-sidecar/app/search.py, ceit-ai-sidecar/tests/test_filters.py, ceit-ai-sidecar/tests/test_api.py</files>
  <read_first>ceit-ai-sidecar/app/search.py (rrf_search at 90, passes() at 109-125 — existing paper_type/department/publication_year/year_from/year_to clauses at 112-121), ceit-ai-sidecar/tests/test_filters.py (the full file — _build_index 12-36, test_filter_paper_type_and_year_range 49-63), ceit-ai-sidecar/tests/test_api.py (client fixture 16-53, 200-shape asserts 68-77, 422 posture tests 85-94), .planning/phases/11-academic-papers-agentic-search/11-PATTERNS.md (section 13 + 17-18), .planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (Code Examples 2, pitfalls #5/#6, A-2/A-3)</read_first>
  <action>
  - In `ceit-ai-sidecar/app/search.py`, `passes()` (109-125): add module-level `AUTHOR_KEYS = ("research_adviser", "technical_adviser")` and two additive clauses BEFORE the final corpus check:
    - `if f.get("author"): needle = str(f["author"]).lower(); if not any(needle in str(name).lower() for name in (meta.get("authors") or [])): return False`
    - `if f.get("adviser"): needle = str(f["adviser"]).lower(); if not any(needle in str(meta.get(k) or "").lower() for k in AUTHOR_KEYS): return False`
    - Semantics locked: case-insensitive SUBSTRING; adviser = research OR technical; every value str()-guarded; `authors` may be absent (default `[]`).
  - No signature changes to `rrf_search` (90-107); existing clauses verbatim.
  - In `ceit-ai-sidecar/tests/test_filters.py`:
    - `test_filter_author_any_of_multiple`: `filters={"author": "juan"}` → every returned doc has a case-insensitive substring match in `metadata.authors`; docs whose authors do NOT contain the needle are excluded; combined with `year_from`/`year_to` the intersection is correct.
    - `test_filter_adviser_research_or_technical`: `filters={"adviser": "engr. jose"}` returns docs whose `metadata.research_adviser` OR `metadata.technical_adviser` contains it — including at least one research-only and one technical-only match (per Task 1 fixtures).
    - `test_filter_author_non_string_junk_does_not_raise`: pass `filters={"author": ["x"]}` / `{"adviser": 42}` → clean filtered result or empty set, NO TypeError.
  - In `ceit-ai-sidecar/tests/test_api.py`: `test_search_endpoint_accepts_author_adviser_filters` — POST /search with `{"query": "...", "filters": {"author": "juan", "adviser": "engr. jose"}, "corpus": "catalog", "limit": 10, "k": 60}` + token header → 200 with `results` list containing only author+adviser-matching ids. In the SAME commit, keep the existing unknown-top-level-field 422 test green and assert it still 422s with an `author` key at TOP level (posture unchanged).
  </action>
  <behavior>
  GIVEN an index with rich-fixture docs (Task 1)
  WHEN `rrf_search(query, filters={"author": "juan"}, ...)` runs
  THEN every result doc has an author whose name contains "juan" (case-insensitive), and docs without it are excluded
  WHEN `rrf_search(query, filters={"adviser": "engr. jose"}, ...)` runs
  THEN every result doc matches "engr. jose" in research_adviser OR technical_adviser (either role)
  WHEN filters values are non-string junk
  THEN no exception escapes — results are filtered-out or empty.
  </behavior>
  <verify>cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run pytest tests/test_filters.py tests/test_api.py -x</verify>
  <acceptance_criteria>
  - `ceit-ai-sidecar/app/search.py` contains `AUTHOR_KEYS = ("research_adviser", "technical_adviser")` and the two `if f.get("author")` / `if f.get("adviser")` clauses inside `passes()`
  - `ceit-ai-sidecar/tests/test_filters.py` contains `test_filter_author_any_of_multiple`, `test_filter_adviser_research_or_technical`, `test_filter_author_non_string_junk_does_not_raise`
  - `ceit-ai-sidecar/tests/test_api.py` contains `test_search_endpoint_accepts_author_adviser_filters`; the unknown-top-level-field 422 test still passes unchanged
  - `uv run pytest tests/test_filters.py tests/test_api.py -x` exits 0
  - `uv run ruff check app/search.py tests/test_filters.py tests/test_api.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Papers golden-set cases + snapshot refresh + eval quality gate</name>
  <files>ceit-ai-sidecar/data/golden_dataset.json, ceit-ai-sidecar/app/eval.py (run only)</files>
  <read_first>ceit-ai-sidecar/data/golden_dataset.json (current shape: version, catalog_snapshot, test_cases with query/corpus/relevant_docs, optional filters/negative), ceit-ai-sidecar/app/eval.py (load_golden 22, case execution 29-58, negative handling 42-44/71-72, main 84+ — --corpus catalog + filters + negatives already supported), C:\Users\admin\Herd\CEIT-Library\storage\app\ai-corpus\catalog.json (the CURRENTLY exported corpus — source of truth for real paper ids/names), .planning/phases/11-academic-papers-agentic-search/11-RESEARCH.md (A-6, Validation Architecture quality-gate row, Wave-0 gap #3)</read_first>
  <action>
  - Add to `C:\Users\admin\Herd\ceit-ai-sidecar\data\golden_dataset.json` `test_cases` array, with `"corpus": "catalog"` and `"filters"` where needed, ~10 paper cases covering all four search axes + 2 negatives:
    - author: `{"query": "<author name from catalog.json>", "filters": {"author": "<name>"}, "relevant_docs": ["paper-N", ...]}` (expected ids derived from the exported corpus metadata)
    - adviser: `{"query": "<adviser name>", "filters": {"adviser": "<name>"}, "relevant_docs": [...]}`
    - year range: existing-style query + `"filters": {"year_from": ..., "year_to": ...}`
    - topic: free-text over composed doc (e.g. department+paper_type keyword query)
    - negatives: `{"query": "<fabricated topic>", "corpus": "catalog", "negative": true, "relevant_docs": []}` — must retrieve zero relevant ids
  - Refresh `catalog_snapshot` to the timestamp of the corpus the cases were derived from (11-01's re-export) — same commit as the cases (Wave-0 gap #3).
  - Derive expected ids ONLY from `storage/app/ai-corpus/catalog.json` ids (`paper-N`), never from conftest fixtures.
  - REBUILD FIRST (RESEARCH pitfall #9 ordering — export → rebuild → eval): after 11-01's re-export lands, rebuild the sidecar index so the eval measures the NEW rich doc shape, not the stale title-doubled index: run `php artisan ai:sync-index` from `C:\Users\admin\Herd\CEIT-Library` (or `POST /index/rebuild` with the `X-Sidecar-Token`) and confirm `/health` reports the rebuilt version before running the gate.
  - Run the gate: `python -m app.eval --corpus catalog` — report the printed P@k/R@k/F1 + negative pass rate; every positive case must hit its expected ids, negatives must pass (zero relevant retrieved). If a case fails, fix the case's expected ids to the actual exported corpus (the golden set documents the real corpus, it is not aspirational).
  </action>
  <verify>cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run python -m app.eval --corpus catalog</verify>
  <acceptance_criteria>
  - `ceit-ai-sidecar/data/golden_dataset.json` contains ≥ 10 new catalog cases: ≥ 2 author, ≥ 1 adviser, ≥ 2 year-range, ≥ 3 topic, ≥ 2 negative; all with `corpus` = `catalog` and ids matching exported corpus ids
  - `catalog_snapshot` updated to the current export timestamp
  - `uv run python -m app.eval --corpus catalog` exits 0 and reports negative pass rate 100% and every positive paper case hitting its expected ids
  - Sidecar git repo clean after the commit (`git -C C:\Users\admin\Herd\ceit-ai-sidecar status --porcelain` empty)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run pytest tests/` — full sidecar suite green (49 passed / 1 skipped baseline + new tests)
- [ ] `cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run ruff check app/ tests/` — exits 0
- [ ] `uv run python -m app.eval --corpus catalog` — paper golden cases pass, negatives pass, snapshot matches the current export
- [ ] `git -C C:\Users\admin\Herd\ceit-ai-sidecar status --porcelain` — empty (all commits landed in the sidecar repo)
- [ ] Top-level 422 posture verified: `/search` with a stray top-level key still returns `invalid_request`
</verification>

<success_criteria>
- Tasks 1-2 complete: `/search` filters author/adviser live with locked semantics; conftest corpus matches 11-01's exporter shape (wave-0 agreement); endpoint + engine + posture tests green
- Task 3 complete: papers golden cases + refreshed snapshot land; eval quality gate passes — SEARCH-05's retrieval half is measurable
- 11-04's paper tab posts exactly `author`/`adviser` keys and gets correct filtered results
</success_criteria>

<output>
After completion, create `.planning/phases/11-academic-papers-agentic-search/11-02-SUMMARY.md`
</output>
