---
phase: 08-hybrid-search-foundation
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - .planning/phases/08-hybrid-search-foundation/08-golden-mapping.md
  - ceit-ai-sidecar/data/golden_dataset.json
  - ceit-ai-sidecar/app/eval.py
autonomous: false
requirements: [SEARCH-01]
must_haves:
  truths:
    - "Search quality is measurable from day one: `uv run eval --limit 5` prints per-case precision@5/recall@5/F1 against the golden set (success criterion 4)"
    - "Golden set is id-based (`paper-{id}` / `policy-h{id}`), snapshot-stamped with the export's generated_at, and reviewed by the user (D-19)"
    - "Negatives are scored as pass = zero relevant-ids retrieved, reported as a separate negative_pass_rate metric"
  artifacts:
    - path: ceit-ai-sidecar/data/golden_dataset.json
      provides: "25-case bootstrap golden set covering exact title, catalog code, paraphrase, people, Taglish, policy, negative"
      contains: "relevant_docs"
    - path: ceit-ai-sidecar/app/eval.py
      provides: "Golden-set runner CLI (P@k/R@k/F1 + negative pass rate + --json)"
      contains: "precision"
  key_links:
    - from: ceit-ai-sidecar/app/eval.py
      to: ceit-ai-sidecar/data/golden_dataset.json
      via: "eval CLI loads golden_dataset.json and matches on doc ids"
      pattern: "golden_dataset"
---

<objective>
Finalize the bootstrap golden set (D-19/D-20) against the REAL exported catalog and ship the eval CLI that measures hybrid search quality — proving success criterion 4 (search quality measurable against a bootstrap golden set from day one) and gating search quality before any UI work.

Purpose: Without the golden set + eval runner, hybrid search quality is unverifiable and RRF/filter tuning would overfit invisibly. This plan converts the 25-case draft in 08-RESEARCH.md §6 into an id-based, snapshot-stamped golden set the user reviews, then ports `evaluation_cli.py` (id-matching instead of title-matching) and records the §6 quality gates.

Output: `08-golden-mapping.md` (draft→real-id mapping), `data/golden_dataset.json` (user-approved), `app/eval.py`, and a recorded gate report.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/08-hybrid-search-foundation/08-RESEARCH.md
@.planning/phases/08-hybrid-search-foundation/08-CONTEXT.md
@.planning/phases/08-hybrid-search-foundation/08-PATTERNS.md
@.planning/phases/08-hybrid-search-foundation/08-01-PLAN.md
@.planning/phases/08-hybrid-search-foundation/08-02-PLAN.md
</context>

<threat_model>
ASVS L1. Block on HIGH severity threats.

| Threat | Severity | Mitigation in this plan |
|---|---|---|
| T-01 Golden set carries real student names (catalog is public library data per D-02; set includes author/adviser names in queries) | MED | Golden data lives ONLY in the private `ceit-ai-sidecar` repo (`data/golden_dataset.json`); never copied into the Laravel repo's public artifacts; never sent to external services (eval is fully local — no LLM calls). |
| T-02 Golden-set rot / overfitting to the index (PITFALLS §9) | LOW | `catalog_snapshot` stamped with export `generated_at`; id-based matching (stable ids); held-out cases {8, 12, 15, 21, 25} excluded from any k/pool tuning. |
| T-03 xlsx guide misuse (D-21: NEVER use `CEIT ACADEMIC PAPER.xlsx` rows as production data) | MED | Mapping task uses ONLY the real `catalog.json` export for ids; xlsx is used solely as a shape/realism reference for the §6 draft patterns. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Export real corpus and map draft golden cases to real DB ids</name>
  <files>.planning/phases/08-hybrid-search-foundation/08-golden-mapping.md</files>
  <read_first>.planning/phases/08-hybrid-search-foundation/08-RESEARCH.md, .planning/phases/08-hybrid-search-foundation/08-01-PLAN.md, .planning/phases/08-hybrid-search-foundation/08-CONTEXT.md</read_first>
  <action>In CEIT-Library: run `php artisan ai:export-corpus` (from 08-01). Read `storage/app/ai-corpus/catalog.json` and `policies.json` (programmatic scan — do not dump contents into chat). Produce `.planning/phases/08-hybrid-search-foundation/08-golden-mapping.md` with a table mapping each draft ref P1..P7, H2-R1..R3, H3-R1..R2 (08-RESEARCH §6) to REAL ids: `paper-{id}` via `catalog_code` match (P1→CEIT-IT-25-02 etc.); if a code has no real row, pick the closest real row by title keywords/dept/type and NOTE the substitution explicitly. Also map policy refs to real `policy-h{id}`/`policy-h{id}-r{id}` ids by header titles ("II. Borrowing Policies", "III. Library Hours and Conduct" or closest real titles — note substitutions). Record the export `generated_at` value for the snapshot stamp. Add a per-case column listing which real title/code/dept backs each relevant_docs entry. Use `CEIT ACADEMIC PAPER.xlsx` ONLY as a pattern guide for name/format realism — never as data (D-21).
  </action>
  <verify>Test-Path ".planning/phases/08-hybrid-search-foundation/08-golden-mapping.md"</verify>
  <acceptance_criteria>
    - `08-golden-mapping.md` exists with a P1..P7 + policy-ref → real `paper-{id}`/`policy-h...` mapping table
    - Every substitution (where no real row matches a draft pattern) is explicitly labeled "SUBSTITUTION:" with the chosen real row
    - The file records the export `generated_at` value used for `catalog_snapshot`
    - No xlsx row is used as a production id — all ids come from `catalog.json`/`policies.json`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Draft data/golden_dataset.json (25 id-based cases)</name>
  <files>ceit-ai-sidecar/data/golden_dataset.json</files>
  <read_first>.planning/phases/08-hybrid-search-foundation/08-golden-mapping.md, .planning/phases/08-hybrid-search-foundation/08-RESEARCH.md, ceit-ai-sidecar/app/search.py</read_first>
  <action>Write `ceit-ai-sidecar/data/golden_dataset.json` with shape: `{"version": 1, "catalog_snapshot": "<generated_at from Task 1>", "test_cases": [...]}` — all 25 cases from 08-RESEARCH §6 table, with `relevant_docs` replaced by REAL ids from the mapping: cases 1-18, 25 → `corpus: "catalog"`; cases 19-21, 24 → `corpus: "policy"`; case 22 → catalog; case 23 → catalog+policy (corpus null = union). `filters` object on cases 16-18 (dept=IT / paper_type=Capstone / year=2025+dept=IT). `negative: true` on cases 22, 23, 24 with empty `relevant_docs`. Held-out set {8, 12, 15, 21, 25} stays IN the file (used for eval reporting, excluded only from tuning). Each case keeps the §6 query string exactly (including Taglish).
  </action>
  <verify>cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run python -c "import json; d=json.load(open('data/golden_dataset.json')); print(len(d['test_cases']), d['version'])"</verify>
  <acceptance_criteria>
    - `data/golden_dataset.json` parses as JSON with keys `version` (=== 1), `catalog_snapshot`, `test_cases`
    - `len(test_cases) === 25`; exactly 3 cases have `negative: true` (22, 23, 24); negative cases have empty `relevant_docs`
    - All `relevant_docs` ids match the pattern `paper-` or `policy-h` from the real export (no `paper-N` placeholders remain)
    - Cases 16-18 carry `filters` objects matching the §6 table (dept/type/year)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Draft bootstrap golden set — 25 id-based cases written to ceit-ai-sidecar/data/golden_dataset.json, mapped from the real catalog export (mapping doc: .planning/phases/08-hybrid-search-foundation/08-golden-mapping.md)</what-built>
  <how-to-verify>Open data/golden_dataset.json (sidecar repo). Review the 25 cases: queries feel like real student questions, relevant_docs point at genuinely matching papers/policies, substitutions are acceptable, negatives are things the catalog truly should not match.</how-to-verify>
  <resume-signal>Type "approved" or list case numbers to adjust</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement app/eval.py and record quality gates</name>
  <files>ceit-ai-sidecar/app/eval.py</files>
  <read_first>WSL ~/workspace/rag-search-engine/cli/evaluation_cli.py, .planning/phases/08-hybrid-search-foundation/08-RESEARCH.md, ceit-ai-sidecar/app/search.py</read_first>
  <action>Port `evaluation_cli.py` to `ceit-ai-sidecar/app/eval.py` with id-matching (NOT title-matching — titles collide, ids stable):
  - argparse: `--limit` (int, default 5), `--corpus` (catalog|policy|all), `--json` (flag).
  - For each test case: `HybridSearch.rrf_search(query, k=60, limit=limit, filters=..., corpus=...)` → `precision = |retrieved ∩ relevant| / limit`, `recall = |retrieved ∩ relevant| / |relevant|`, `F1`; negative cases scored as `pass = zero relevant-ids retrieved` aggregated into `negative_pass_rate` (separate metric, not P/R).
  - Print per-case report + category summary rows (exact_title, catalog_code, paraphrase, people, taglish, policy, negative) + overall summary; `--json` emits machine-readable report for CI.
  - Then run: `uv run eval --limit 5` and check §6 gates: catalog exact-title/code P@5 = 1.0; paraphrase/topic P@5 ≥ 0.6; Taglish ≥ 0.6; policy P@5 ≥ 0.7; negative pass rate = 1.0. Tuning (BM25 params / pool decisions only — never the held-out cases) allowed up to 3 iterations; record every iteration's results and the final gate table in the plan SUMMARY.
  </action>
  <verify>cd C:\Users\admin\Herd\ceit-ai-sidecar; uv run eval --limit 5 --json | uv run python -c "import sys,json; json.load(sys.stdin); print('json ok')"</verify>
  <acceptance_criteria>
    - `app/eval.py` matches on `r["id"]` against `relevant_docs` (grep: no title-string comparison)
    - `uv run eval --limit 5` exits 0 and prints per-case `precision`, `recall`, `F1` plus a `negative_pass_rate` line
    - `uv run eval --limit 5 --json` outputs valid JSON
    - Final gate table recorded in `08-03-SUMMARY.md` with per-category P@5 and the held-out split noted
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `data/golden_dataset.json` is user-approved (checkpoint resumed "approved")
- [ ] `uv run eval --limit 5` in the sidecar prints the gate report
- [ ] Gate check: exact-title/code P@5 = 1.0; paraphrase ≥ 0.6; Taglish ≥ 0.6; policy ≥ 0.7; negative pass rate = 1.0 (or tuning iterations documented to their final values)
- [ ] Sidecar pytest still green (`uv run pytest -q`)
</verification>

<success_criteria>
- All tasks complete; checkpoint approved by user (D-19 review obligation met)
- Bootstrap golden set exists id-based + snapshot-stamped in the sidecar repo
- Search quality quantified with P@5/R@5/F1 + negative pass rate — the phase's success criterion 4 is demonstrably satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/08-hybrid-search-foundation/08-03-SUMMARY.md`
</output>
