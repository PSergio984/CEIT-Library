---
phase: 08-hybrid-search-foundation
plan: 05
type: execute
wave: 2
depends_on: ["08-04"]
files_modified:
  - app/Livewire/Pages/Student/AcademicPaperIndex.php
  - resources/views/livewire/pages/student/academic-paper-index.blade.php
  - tests/Feature/AcademicPaperIndexHybridTest.php
autonomous: true
requirements: [SEARCH-01]
must_haves:
  truths:
    - "Typing ≥ 3 chars in the papers-page search box triggers hybrid search via AiService (corpus: catalog, limit 10) and renders cards ordered by sidecar score — success criterion 1's user-visible surface"
    - "paper_type / department / year dropdown filters are forwarded to the sidecar as `filters` and narrow hybrid results"
    - "Sidecar down → page falls back to the existing local SQL search with the notice text 'AI search unavailable — showing basic results' (no 500, no UX regression)"
  artifacts:
    - path: app/Livewire/Pages/Student/AcademicPaperIndex.php
      provides: "runHybridSearch() action wiring search box + filters to AiService"
      contains: "AiService::search"
    - path: resources/views/livewire/pages/student/academic-paper-index.blade.php
      provides: "Hybrid result card rendering + fallback notice"
      contains: "AI search unavailable"
    - path: tests/Feature/AcademicPaperIndexHybridTest.php
      provides: "Sidecar-ordered rendering + fallback regression tests"
      contains: "hybridResults"
  key_links:
    - from: app/Livewire/Pages/Student/AcademicPaperIndex.php
      to: app/Services/AiService.php
      via: "runHybridSearch() calls AiService::search($search, $filters, 'catalog', 10)"
      pattern: "AiService::search"
    - from: app/Livewire/Pages/Student/AcademicPaperIndex.php
      to: app/Models/AcademicPaper.php
      via: "hydrates AcademicPaper::with(['authors','copies'])->findMany(ids) ordered by sidecar score"
      pattern: "findMany"
---

<objective>
Wire the existing academic papers browse page (`AcademicPaperIndex`) to hybrid search: a `runHybridSearch()` action (NOT inside `#[Computed]` — no HTTP in computed props) calls `AiService::search` with the existing filter dropdown values, hydrates full `AcademicPaper` models ordered by sidecar score, renders sidecar-ordered cards, and gracefully falls back to the current local SQL search when the sidecar is unavailable.

Purpose: This is the user-visible surface of SEARCH-01 (natural-language catalog search with filters) and success criterion 1. Availability/status filters stay SQL-only (D-04); no chat UI (D-09).

Output: Modified `AcademicPaperIndex` Livewire component + its blade view, plus `AcademicPaperIndexHybridTest`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-hybrid-search-foundation/08-RESEARCH.md
@.planning/phases/08-hybrid-search-foundation/08-PATTERNS.md
@.planning/phases/08-hybrid-search-foundation/08-04-PLAN.md
</context>

<threat_model>
ASVS L1. Block on HIGH severity threats.

| Threat | Severity | Mitigation in this plan |
|---|---|---|
| T-01 XSS via catalog titles/names rendered into cards | HIGH | All titles/codes/names rendered with Blade `{{ }}` escaping (existing convention); NO `{!! !!}` for any catalog-derived value. Hybrid card markup mirrors the existing escaped card blocks. |
| T-02 Hybrid path bypasses existing search rate limiting | MED | `runHybridSearch()` is only reachable through `updatedSearch` on the papers route already behind the `throttle:search` rate limiter and existing role gates; no new route, no auth change. |
| T-03 Hung sidecar wedges the Livewire request | MED | Bounded by `AiService` timeouts (08-04); catch of `AiServiceUnavailableException` + `AiServiceAuthException` converts failure into the SQL fallback + notice — page always renders. |
| T-04 Hybrid results claim availability (D-04 anti-hallucination) | MED | Hybrid cards render ONLY title/code/badges/filters — status/availability badges continue to come from the existing local DB path; sidecar metadata has no availability fields to render. |
| T-05 Debounce mismatch (blade uses 300ms, research text says 400ms) | LOW | Keep the existing `wire:model.live.debounce.300ms` in `academic-paper-filters.blade.php` (locked agent discretion per PATTERNS); do NOT change the debounce value. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire runHybridSearch() into AcademicPaperIndex Livewire component</name>
  <files>app/Livewire/Pages/Student/AcademicPaperIndex.php</files>
  <read_first>app/Livewire/Pages/Student/AcademicPaperIndex.php, app/Services/AiService.php, resources/views/components/academic-paper-filters.blade.php, .planning/phases/08-hybrid-search-foundation/08-RESEARCH.md, .planning/phases/08-hybrid-search-foundation/08-PATTERNS.md</read_first>
  <action>Modify `app/Livewire/Pages/Student/AcademicPaperIndex.php`:
  - Add `public ?array $hybridResults = null;` and `public bool $aiSearchFailed = false;` (drives the fallback notice).
  - Add `public function runHybridSearch(): void` — guard `strlen(trim($this->search)) >= 3` else reset `$hybridResults = null; $aiSearchFailed = false; return;`. Build `$filters = ['paper_type' => $this->paperTypeFilter ?: null, 'department' => $this->departmentFilter ?: null, 'publication_year' => $this->yearFilter ?: null, 'year_from' => $this->yearFromFilter ?: null, 'year_to' => $this->yearToFilter ?: null]` from the EXISTING filter props (do not rebuild those dropdowns). Call `AiService::search($this->search, $filters, 'catalog', 10)` inside try/catch of `AiServiceUnavailableException` + `AiServiceAuthException` → on failure set `$hybridResults = null; $aiSearchFailed = true;` and return (existing SQL computed path keeps rendering). On success: strip `paper-` prefix from each result `id` → int ids; `AcademicPaper::with(['authors', 'copies'])->findMany($ids)`; map to `$hybridResults` preserving the SIDECAR order (array ordered by sidecar rank, not DB id).
  - `updatedSearch()`: keep `$this->resetPage('academic-papers-index');` then call `$this->runHybridSearch();` when search ≥ 3 chars, else clear hybrid state.
  - Keep `academicPapers()` `#[Computed]` UNTOUCHED (local LIKE + availability logic stays the fallback and the availability source — D-04); NO HTTP calls inside computed properties.
  - No changes to pagination page name (`academic-papers-index`), `#[Title]`/`#[Layout]`/`#[Lazy]` attributes, or `CreatesQrCanonicalMessage`.
  </action>
  <verify>php -l app/Livewire/Pages/Student/AcademicPaperIndex.php</verify>
  <acceptance_criteria>
    - `AcademicPaperIndex.php` contains `runHybridSearch()` calling `AiService::search` with args `($this->search, $filters, 'catalog', 10)`
    - Contains `catch` for `AiServiceUnavailableException` (and `AiServiceAuthException`); sets `$aiSearchFailed = true` on failure
    - `#[Computed]` method `academicPapers()` contains no `AiService` reference
    - Contains `->findMany(` and preserves sidecar order (no `sortBy` on results)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render hybrid result cards + fallback notice in blade view</name>
  <files>resources/views/livewire/pages/student/academic-paper-index.blade.php</files>
  <read_first>resources/views/livewire/pages/student/academic-paper-index.blade.php, resources/views/components/academic-paper-filters.blade.php, .planning/phases/08-hybrid-search-foundation/08-PATTERNS.md</read_first>
  <action>Modify the papers-page blade:
  - When `!is_null($this->hybridResults)` render the hybrid card list INSTEAD of the existing `@forelse ($this->academicPapers ...)` loop, in BOTH the mobile and desktop sections — mirror the existing card markup exactly (daisyUI `bg-base-100 border-base-300 rounded-lg shadow-sm` classes, `badge` for status from the LOCAL DB row, `badge-outline` for `catalog_code`, `line-clamp-2` title, dept/year/paper_type line), `wire:key="hybrid-mobile-{{ $paper->id }}"` / `wire:key="hybrid-desktop-{{ $paper->id }}"`, reuse `showPaperDetails($paper->id)` action. All values escaped with `{{ }}` (never `{!! !!}`).
  - Add a notice element: when `$this->aiSearchFailed` is true render `AI search unavailable — showing basic results` (exact string) as a subtle alert (daisyUI `alert`/`text-sm` style); when hybrid results active render nothing extra (or a subtle `AI search results` badge — agent discretion).
  - Do NOT modify `academic-paper-filters.blade.php` (debounce stays 300ms) and do NOT touch the existing SQL-path markup beyond wrapping it in the `@if (is_null($this->hybridResults))` branch.
  </action>
  <verify>php artisan view:cache</verify>
  <acceptance_criteria>
    - Blade contains the literal string `AI search unavailable` and `showing basic results`
    - Blade contains `wire:key="hybrid-mobile-` and `wire:key="hybrid-desktop-`
    - Blade contains `$this->hybridResults` branch and the existing SQL `@forelse` is preserved inside the fallback branch
    - No `{!! !!}` introduced for catalog-derived values
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Write AcademicPaperIndexHybridTest feature tests</name>
  <files>tests/Feature/AcademicPaperIndexHybridTest.php</files>
  <read_first>tests/fixtures/ai-sidecar/search.json, tests/Feature/Livewire/QrScannerTest.php, app/Livewire/Pages/Student/AcademicPaperIndex.php, .planning/phases/08-hybrid-search-foundation/08-RESEARCH.md</read_first>
  <action>Create `tests/Feature/AcademicPaperIndexHybridTest.php`:
  - Seed 2+ `AcademicPaper` rows (factories) matching fixture ids in `tests/fixtures/ai-sidecar/search.json`.
  - `Http::fake(['http://127.0.0.1:8310/search' => Http::response(fixture, 200)])`; `Livewire::test(AcademicPaperIndex::class)->set('search', 'water pump')->call('runHybridSearch')` → `assertSet('hybridResults', fn ($v) => ...)`; assert the page renders the sidecar-ordered order (first title in DOM is the fixture's first result, NOT the lowest DB id) — `assertSeeInOrder` with sidecar order.
  - Filters forwarded: `->set('departmentFilter', 'Information Technology')` → `Http::assertSent` captures request body `filters.department === 'Information Technology'`.
  - Fallback (no 500): `Http::fake(fn () => throw new ConnectionException(...))` → call `runHybridSearch` → assert no exception propagates, `aiSearchFailed` is true, page still renders the SQL-path paper list, and the blade shows `AI search unavailable` text.
  - Short query guard: `set('search', 'ab')` → runHybridSearch → `Http::assertNothingSent()`, hybridResults null.
  </action>
  <verify>php artisan test --filter=AcademicPaperIndexHybridTest</verify>
  <acceptance_criteria>
    - `php artisan test --filter=AcademicPaperIndexHybridTest` exits 0
    - Test file contains `assertSeeInOrder` for sidecar-ordered rendering and a `ConnectionException` fallback test
    - Test file asserts `filters` body key forwarding (`filters` + department value)
    - Test asserts no HTTP sent when search length < 3
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `php artisan test --filter=AcademicPaperIndexHybridTest` — green
- [ ] Full regression: `php artisan test` — existing suite stays green (existing papers-page tests untouched in behavior)
- [ ] Manual UAT (08-RESEARCH §8.4 item 3): with sidecar running — exact title → top result; catalog code → rank 1; Taglish paraphrase → relevant hit; dropdown filters narrow; kill sidecar → page falls back with notice
</verification>

<success_criteria>
- All 3 tasks complete
- Papers page serves hybrid results ordered by sidecar score with filters, and degrades to SQL search + notice when the sidecar is down
- SEARCH-01's in-app surface (natural-language search with paper_type/department/year filters) is demonstrable end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/08-hybrid-search-foundation/08-05-SUMMARY.md`
</output>
