---
phase: 03-stability-and-performance
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - config/app.php
  - public/manifest.json
  - resources/views/pages/auth/login.blade.php
  - resources/views/pages/auth/register.blade.php
  - resources/views/pages/auth/forgot-password.blade.php
  - resources/views/pages/auth/reset-password.blade.php
  - resources/views/components/layouts/app.blade.php
  - resources/views/mail/welcome.blade.php
  - resources/views/mail/librarian-assignment-reminder.blade.php
  - documentations/TEST_CASES.md
autonomous: false
requirements: [REQ-02]
must_haves:
  truths:
    - Global app name is exactly "PLV CEIT Library"
    - Auth pages share the Liquid Glass aesthetic with the landing page
  artifacts:
    - path: resources/views/pages/auth/login.blade.php
      provides: Reskinned login page
    - path: config/app.php
      provides: Global configuration update
  key_links: []
---

<objective>
Finalize the global branding update and apply the new Liquid Glass aesthetic to all Auth pages for a cohesive user journey.

Purpose: To ensure the "PLV CEIT Library" branding is globally consistent (title tags, PWA manifest, config) and that moving from the premium landing page to login/registration does not break the visual immersion.
Output: Reskinned Auth views, updated layouts with SEO tags, and updated global configurations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stability-and-performance/03-CONTEXT.md
@.planning/phases/03-stability-and-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global Branding & SEO Update</name>
  <files>config/app.php, public/manifest.json, resources/views/components/layouts/app.blade.php, resources/views/mail/welcome.blade.php, resources/views/mail/librarian-assignment-reminder.blade.php, documentations/TEST_CASES.md</files>
  <action>Update the application name configuration to exactly "PLV CEIT Library". Update `config/app.php` fallback to match. In `public/manifest.json`, update `name` and `short_name`. Add tasks/actions to update email layouts/templates (`resources/views/mail/*`), documentation files (e.g. `TEST_CASES.md`), and layout files (`resources/views/components/layouts/app.blade.php`) to reflect the new name 'PLV CEIT Library' and add SEO meta tags and descriptions (focused on "PLV Ceit Library"). Ensure `tests/Feature/PageTitleTest.php` passes with the new branding (it might need an assertion update if it expects the old name).</action>
  <verify>
    <automated>php artisan test --filter PageTitleTest</automated>
  </verify>
  <done>Global configurations, emails, layouts, and documentation updated and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Auth Pages</name>
  <files>resources/views/pages/auth/login.blade.php, resources/views/pages/auth/register.blade.php, resources/views/pages/auth/forgot-password.blade.php, resources/views/pages/auth/reset-password.blade.php</files>
  <action>Apply the "Liquid Glass" theme from the landing page to the Auth views. Maintain the existing Livewire/Volt logic but wrap the forms in the translucent glass panels (`bg-slate-900/60 backdrop-blur-md`) over the fixed `plvbg.jpg` background. Ensure text contrast and form field legibility within the glassmorphism theme.</action>
  <verify>
    <automated>php artisan test --filter Auth</automated>
  </verify>
  <done>Auth views are reskinned without breaking submission logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Global branding and Liquid Glass aesthetic on Landing and Auth pages.</what-built>
  <how-to-verify>
    1. Visit the root URL and verify the premium landing page design, scrolling animations, and title tag.
    2. Click 'Login' and verify the design remains cohesive (Liquid Glass theme).
    3. Click 'Register' and verify design.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Auth Views | The login/register views handle credentials; visual reskinning must not remove CSRF tokens or Livewire model bindings. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-1 | Spoofing | Auth Forms | mitigate | Retain existing Livewire form submission logic and error messaging precisely as they are. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty` to ensure code formatting complies with Laravel guidelines.
Run `php artisan test` to ensure all tests across the entire suite are now passing, completing the phase goal.
</verification>

<success_criteria>
All tests are green. The user approves the new aesthetic. "PLV CEIT Library" is the global application name, and Pint formatting is clean.
</success_criteria>

<output>
Create `.planning/phases/03-stability-and-performance/03-04-SUMMARY.md` when done
</output>