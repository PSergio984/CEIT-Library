---
phase: 03-stability-and-performance
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - tests/Feature/WelcomePageTest.php
  - resources/views/welcome.blade.php
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - Landing page has a premium "Liquid Glass" theme with parallax background
    - Scroll-driven animations are present using Alpine.js (`x-intersect`)
    - WelcomePageTest asserts page load and new branding presence
  artifacts:
    - path: resources/views/welcome.blade.php
      provides: Premium landing page
    - path: tests/Feature/WelcomePageTest.php
      provides: Test coverage for landing page
  key_links: []
---

<objective>
Transform the entry point into a premium, high-conversion landing page using a "Liquid Glass" aesthetic while retaining the existing school background image.

Purpose: To create a "Wow" aesthetic for the PLV CEIT Library entry point, establishing a premium look and feel before users log in.
Output: An updated `welcome.blade.php` and its corresponding test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stability-and-performance/03-CONTEXT.md
@.planning/phases/03-stability-and-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Landing Page Test</name>
  <files>tests/Feature/WelcomePageTest.php</files>
  <action>Create a test that verifies the welcome page returns a 200 status, displays the text "PLV CEIT Library", and contains the necessary structural components of the new landing page (e.g. login link, register link, Hero section text).</action>
  <behavior>
    - Test 1: Welcome page renders correctly with status 200.
    - Test 2: Welcome page contains "PLV CEIT Library".
  </behavior>
  <verify>
    <automated>php artisan test --filter WelcomePageTest</automated>
  </verify>
  <done>Failing test is committed, ready for implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Liquid Glass Landing Page</name>
  <files>resources/views/welcome.blade.php</files>
  <action>Overhaul the welcome page. Implement a "Liquid Glass" theme using Tailwind CSS v4 and Alpine.js. Retain the existing `plvbg.jpg` as the base layer for a parallax effect. Overlay translucent glass panels (e.g., `bg-slate-900/60 backdrop-blur-md`). Use Alpine's `x-intersect` for scroll-driven staggering reveals in the features section. Match the CEIT color palette (Slate/Blue/Teal). Explicitly include implementing the requested 'Satoshi / General Sans (via DM Sans fallback)' typography with massive scale contrast. Ensure it passes the `WelcomePageTest`.</action>
  <verify>
    <automated>php artisan test --filter WelcomePageTest</automated>
  </verify>
  <done>Landing page is fully styled and passes the test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public access | Welcome page is public; ensure no sensitive authenticated data leaks into the unauthenticated view. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-1 | Information Disclosure | Welcome Page | mitigate | Ensure dynamic links correctly check `auth()->check()` before displaying dashboard links versus login links. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty` to ensure code formatting complies with Laravel guidelines.
Run `php artisan test --filter WelcomePageTest` and manually load the application root to verify the visual aesthetic.
</verification>

<success_criteria>
Welcome page test passes, the aesthetic matches the "Liquid Glass" constraints with scroll animations, and Pint formatting is clean.
</success_criteria>

<output>
Create `.planning/phases/03-stability-and-performance/03-03-SUMMARY.md` when done
</output>