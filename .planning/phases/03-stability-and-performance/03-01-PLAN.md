---
phase: 03-stability-and-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Auth/PasswordResetTest.php
  - tests/Feature/EmailVerificationTest.php
  - tests/Feature/ProfileTest.php
  - tests/Feature/RoleBasedAccessControlTest.php
  - tests/Feature/AttendanceLogsTest.php
  - tests/Feature/ManageRolesTest.php
  - tests/Feature/ExportTest.php
  - tests/Feature/SessionManagementTest.php
  - tests/Feature/PasswordValidationTest.php
  - tests/Feature/Security/FrontendSecurityAuditTest.php
  - tests/Feature/SuperAdminSeedConfigTest.php
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - Auth, Profile, and Middleware tests accurately reflect current app behavior
    - Tests expecting 403 correctly handle the 302 redirect logic now used
    - SuperAdmin seeder is idempotent and doesn't trigger duplicate key constraints
  artifacts:
    - path: tests/Feature/ProfileTest.php
      provides: Corrected assertions targeting new Volt layout
    - path: tests/Feature/AttendanceLogsTest.php
      provides: Correct 302 redirect assertions
  key_links:
    - from: tests/Feature/ProfileTest.php
      to: resources/views/pages/profile.blade.php
      via: Volt component lookup
      pattern: Volt::test
---

<objective>
Resolve the first half of the failing test suite (Auth, Profile, Authorization, and Data Seeding) by updating outdated assertions to match the current application state.

Purpose: 50 tests currently fail, largely because the tests assert against old route names, old `403` status codes (app now redirects via `302`), or missing components. We need the test suite completely green to ensure stability.
Output: A passing suite of Auth, Authorization, and Core Backend tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stability-and-performance/03-CONTEXT.md
@.planning/phases/03-stability-and-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Authorization & Middleware Tests</name>
  <files>tests/Feature/RoleBasedAccessControlTest.php, tests/Feature/AttendanceLogsTest.php, tests/Feature/ManageRolesTest.php, tests/Feature/ExportTest.php, tests/Feature/SessionManagementTest.php, tests/Feature/PasswordValidationTest.php, tests/Feature/Security/FrontendSecurityAuditTest.php</files>
  <action>Fix failing assertions where tests expect a `403` response but receive a `302` redirect. Update the assertions from `assertStatus(403)` to `assertRedirect()`. Fix any test using obsolete generic HTTP requests by using `Livewire::test()` or updating expectations to match the current application logic. Do not change the application logic to fit the test.</action>
  <verify>
    <automated>php artisan test --filter RoleBasedAccessControlTest</automated>
  </verify>
  <done>All tests in the specified files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Auth & Profile Tests</name>
  <files>tests/Feature/Auth/PasswordResetTest.php, tests/Feature/EmailVerificationTest.php, tests/Feature/ProfileTest.php</files>
  <action>Update Auth/Profile tests. In `ProfileTest`, update references to old `profile.update-profile-information-form` to target the actual component name used in Volt (or adjust the assertion to just check for page text). In `EmailVerificationTest`, fix closure type hints for mocked mails and remove checks for missing `password.email` route if they now use a different flow. Ensure `PasswordResetTest` correctly tests Livewire/Volt actions rather than standard HTTP form submissions.</action>
  <verify>
    <automated>php artisan test --filter ProfileTest</automated>
  </verify>
  <done>Auth and Profile tests run completely green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Seeder Idempotency Test</name>
  <files>tests/Feature/SuperAdminSeedConfigTest.php</files>
  <action>Update the `SuperAdminSeedConfigTest` (or the underlying seeder it tests) so that running it twice does not throw a duplicate key error for fixed accounts.</action>
  <verify>
    <automated>php artisan test --filter SuperAdminSeedConfigTest</automated>
  </verify>
  <done>Seeder test passes cleanly on repeated runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test assertions | Ensuring we do not accidentally weaken application security by removing tests, only aligning their expectations. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-1 | Tampering | Test Fixes | mitigate | Only modify test assertions, do not lower actual authorization gates in Controllers/Middleware. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty` to ensure code formatting complies with Laravel guidelines.
Run `php artisan test` and verify that the specified files no longer produce failures.
</verification>

<success_criteria>
All targeted test files transition from FAILED to PASS, and Pint formatting is clean.
</success_criteria>

<output>
Create `.planning/phases/03-stability-and-performance/03-01-SUMMARY.md` when done
</output>