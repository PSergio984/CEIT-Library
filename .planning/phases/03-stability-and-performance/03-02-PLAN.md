---
phase: 03-stability-and-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/FiltersTest.php
  - tests/Feature/NavigationTest.php
  - tests/Feature/PageTitleTest.php
  - tests/Feature/TransactionHistoryTest.php
  - tests/Feature/ToastNotificationsTest.php
  - tests/Feature/AttendanceNotificationsTest.php
  - tests/Feature/ViolationNotificationTest.php
  - tests/Feature/Livewire/CreditScoreHistoryTest.php
  - tests/Feature/Livewire/QrScannerFileUploadTest.php
  - tests/Feature/QrCodeScannabilityTest.php
  - app/Livewire/Admin/AcademicPapers.php
  - app/Livewire/Admin/BorrowLogs.php
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - UI tests pass by awaiting lazy-loaded components or targeting Livewire correctly
    - Notification database assertions correctly verify actual data structures
    - Query optimization applied to heavy admin Livewire tables (N+1 fixed)
  artifacts:
    - path: tests/Feature/FiltersTest.php
      provides: Passing lazy-load tests
    - path: app/Livewire/Admin/AcademicPapers.php
      provides: Eager-loaded data reducing N+1 queries
  key_links: []
---

<objective>
Resolve the second half of the failing test suite focusing on Livewire UI, lazy loading, Notifications, and QR functionalities. Additionally, implement database query optimization for heavy Livewire tables.

Purpose: Fix tests that fail because they expect to see table data instantly but encounter lazy-loading placeholders. Optimize queries to fulfill the performance goal of the phase.
Output: A fully green UI and Notification test suite, plus optimized Eloquent queries for admin tables.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-stability-and-performance/03-CONTEXT.md
@.planning/phases/03-stability-and-performance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UI & Lazy Loading Tests</name>
  <files>tests/Feature/FiltersTest.php, tests/Feature/NavigationTest.php, tests/Feature/PageTitleTest.php, tests/Feature/TransactionHistoryTest.php, tests/Feature/Livewire/CreditScoreHistoryTest.php</files>
  <action>Update generic HTTP tests that assert text on lazy-loaded views. Switch these to use `Volt::test()` or `Livewire::test(Component::class)` to bypass the lazy loading state and correctly render the component content. For navigation or title tests, ensure they align with the new routing or Volt structures.</action>
  <verify>
    <automated>php artisan test --filter FiltersTest</automated>
  </verify>
  <done>All tests correctly wait for or target components and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Notifications & QR Tests</name>
  <files>tests/Feature/ToastNotificationsTest.php, tests/Feature/AttendanceNotificationsTest.php, tests/Feature/ViolationNotificationTest.php, tests/Feature/Livewire/QrScannerFileUploadTest.php, tests/Feature/QrCodeScannabilityTest.php</files>
  <action>Fix database assertions in Notification tests to match the actual JSON payload structure stored in the database. In `QrCodeScannabilityTest`, modify or conditionally skip Imagick-dependent PNG assertions if Imagick is not installed, or handle gracefully. Fix Replay Attack assertions in Qr Scanner tests.</action>
  <verify>
    <automated>php artisan test --filter AttendanceNotificationsTest</automated>
  </verify>
  <done>Notification logic assertions run green.</done>
</task>

<task type="auto">
  <name>Task 3: Optimize Database Queries</name>
  <files>app/Livewire/Admin/AcademicPapers.php, app/Livewire/Admin/BorrowLogs.php</files>
  <action>Audit and update the `query()` builders in these heavy Livewire data tables to ensure eager loading (`with('authors', 'inventory')` etc.) is utilized to prevent N+1 query issues.</action>
  <verify>
    <automated>php artisan test --filter AcademicPapersTest</automated>
  </verify>
  <done>Eager loading is applied to relations fetched inside loops or tables.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Query Optimizations | Ensure that applying eager loading does not accidentally bypass global scopes or authorization filtering logic on models. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-1 | Information Disclosure | DB Queries | mitigate | Ensure eager loaded relationships (`with()`) respect user scoping if applicable. |
</threat_model>

<verification>
Run `vendor/bin/pint --dirty` to ensure code formatting complies with Laravel guidelines.
Run `php artisan test` and verify that the UI and Notification test suites pass perfectly.
</verification>

<success_criteria>
All targeted test files transition from FAILED to PASS, eager loading is visibly applied to major admin lists, and Pint formatting is clean.
</success_criteria>

<output>
Create `.planning/phases/03-stability-and-performance/03-02-SUMMARY.md` when done
</output>